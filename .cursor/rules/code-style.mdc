---
description: 
globs: 
alwaysApply: true
---
# Правила написания кода и ответов для AI-агента

## 1. Пиши кратко, объясняй концепции
Твоя главная задача — экономить мое время. Код должен быть лаконичным, а объяснения — четкими и по делу, фокусируясь на ключевых решениях.

**❌ Неправильно (слишком многословно):**

> "Итак, для решения вашей задачи я собираюсь реализовать функцию. Эта функция будет принимать один аргумент, который, как я предполагаю, будет списком целых чисел..."

**✅ Правильно (кратко и по делу):**

> "Вот функция для суммирования элементов списка. Я использую встроенную функцию sum() для максимальной эффективности и читаемости кода."

```python
def get_total_sum(numbers: list[int]) -> int:
    """Возвращает сумму всех чисел в списке."""
    return sum(numbers)
```

---

## 2. Используй полную семантическую разметку
Каждый фрагмент кода должен быть интуитивно понятен без необходимости его запуска.

### 2.1. Контракт функции (сигнатура)
**Правило**: строго аннотируй типы и используй описательные имена.

**❌ Неправильно:**
```python
# обработка данных
def proc(d):
    # ...
```

**✅ Правильно:**
```python
def filter_active_users(users: list[dict]) -> list[dict]:
    # ...
```

### 2.2. Намерение и логика (документация и комментарии)
**Правило**: пиши docstrings и объясняй "почему", а не "что".

**❌ Неправильно:**
```python
def calculate_discount(price, discount):
    # Умножаем цену на скидку
    return price * discount
```

**✅ Правильно:**
```python
def calculate_discounted_price(original_price: float, discount_percentage: int) -> float:
    """
    Вычисляет итоговую цену товара после применения скидки.
    """
    # Бизнес-логика: скидка применяется как доля от цены
    discount_factor = discount_percentage / 100
    return original_price * (1 - discount_factor)
```

### 2.3. "Магические" значения
**Правило**: выноси любые повторяющиеся или имеющие бизнес-смысл значения в константы.

**❌ Неправильно:**
```python
def get_db_connection():
    return create_connection("127.0.0.1", 5432, timeout=10)
```

**✅ Правильно:**
```python
# config.py
DB_HOST = "127.0.0.1"
DB_PORT = 5432
DEFAULT_TIMEOUT_SECONDS = 10

def get_db_connection():
    """Создает и возвращает соединение с БД, используя параметры из конфигурации."""
    return create_connection(DB_HOST, DB_PORT, timeout=DEFAULT_TIMEOUT_SECONDS)
```

---

## 3. Предпочитай структурированные данные
Вместо общих типов (dict) всегда определяй явную структуру: dataclasses, Pydantic.

**❌ Неправильно:**
```python
def get_user_info(user: dict) -> str:
    return f"Имя: {user['name']}, Возраст: {user['age']}"
```

**✅ Правильно:**
```python
from pydantic import BaseModel, Field, constr

class User(BaseModel):
    """
    Представляет модель пользователя в системе с валидацией данных.
    """
    id: int = Field(..., description="ID пользователя", gt=0, example=123)
    name: constr(min_length=2) = Field(..., description="Имя", example="Иван Иванов")
    age: int = Field(..., description="Возраст", gt=0, le=120, example=30)
    email: str | None = Field(default=None, description="Email", example="user@example.com")
```

---

## 4. Пиши тестируемый и безопасный код
Отделяй логику от конфигурации, явно указывай на побочные эффекты.

**❌ Неправильно:**
```python
def save_user_data(user: User):
    with open("C:/data/prod/users.txt", "a") as f:
        f.write(str(user))
```

**✅ Правильно:**
```python
from pathlib import Path

def save_user_data(user: User, output_path: Path) -> None:
    """
    Сохраняет данные пользователя в файл.

    Side Effects:
        - Создает/дописывает файл по пути 'output_path'.

    Args:
        user: Пользователь.
        output_path: Путь к файлу.
    """
    with output_path.open("a", encoding="utf-8") as f:
        f.write(str(user))
```

---


## 5. Комментарии СВЕРХУ файла

Каждый `.py` файл **обязан** начинаться с краткого, но содержательного описания **его назначения**, а также **всех ключевых переменных, функций, классов и констант**, присутствующих в этом файле. Это помогает быстро понять суть кода без необходимости чтения всего тела файла.

### Структура заголовочного комментария

```python
"""
Файл: user_loader.py

Назначение:
    Загрузка и валидация пользовательских данных из внешнего источника (например, CSV или API), с последующей нормализацией и сохранением в базу данных.

Основные компоненты:

Классы:
    - User(BaseModel): Модель пользователя с валидацией полей.

Функции:
    - load_users_from_csv(file_path: Path) -> list[User]:
        Загружает данные из CSV, валидирует каждую запись, возвращает список валидных пользователей.

    - normalize_user(user: User) -> User:
        Приводит поля пользователя к стандартному виду (например, форматирует email, удаляет пробелы).

    - save_users(users: list[User], db_url: str) -> None:
        Сохраняет список пользователей в базу данных, указанную по `db_url`.

Константы:
    - MAX_USERS_PER_BATCH: int = 500
        Максимальное количество пользователей, обрабатываемых за одну итерацию.

    - SUPPORTED_FILE_TYPES: list[str] = [".csv", ".xlsx"]
        Допустимые форматы входных файлов.

Зависимости:
    - pydantic
    - pandas
    - pathlib
"""
```

### Принципы:

1. **Всегда указывай контекст:** не просто "что делает функция", а **зачем она нужна** в рамках общего назначения модуля.
2. **Описывай каждую значимую единицу:** даже если это просто константа или перечисление.
3. **Форматируй читаемо:** не сливай текст в одну портянку, делай визуальные отступы, буллеты, разделяй по категориям (классы, функции, константы).
4. **Обновляй при каждом изменении:** если меняешь сигнатуру или добавляешь новый компонент — обнови докстроку в заголовке.

### Когда допустим пропуск:

Если файл является частью тестов (`test_*.py`) или содержит только импорты (например, `__init__.py`), допускается сокращённый заголовок вида:

```python
"""Инициализация пакета utils."""
```

### Пример плохого заголовка:

```python
"""Функции для пользователей."""
```

❌ Недостаточно информации. Не указано:

- Какие функции
- Какие данные
- Что делает файл

---

### Итог: цель этого пункта — сделать так, чтобы любой разработчик мог открыть файл и за 10 секунд понять:

- Что делает этот файл
- Какие у него есть входные/выходные точки
- Какие ограничения и контексты

> **Важно**: AI, который пишет или дополняет `.py` файл, обязан **генерировать заголовок по этой структуре**, если он отсутствует или неполон.


---


## 6. Добавляй регионы в коде

Используй регионы (code folding) для логической группировки частей кода. Это облегчает навигацию, особенно в длинных файлах, и повышает читаемость. Большинство IDE (например, PyCharm, VSCode) поддерживают скрытие регионов по шаблону `# region ...` / `# endregion`.

---

🔧 **Правила использования:**

- Каждая функция должна быть обёрнута в регион.

- **Формат имени региона:**

  ```python
  # region FUNCTION имя_функции
  def имя_функции(...):
      ...
  # endregion FUNCTION имя_функции
  ```

- Даже для коротких, вспомогательных и `lambda`-функций (если они определены отдельно).

- Имена функций в `# region` и `# endregion` должны совпадать.

- Разделяй код по смысловым блокам:

  - Импорты
  - Константы
  - Классы (каждый класс в своём регионе)
  - Функции (каждая — в отдельном регионе)
  - Конфигурации / параметры
  - Логика `main()`
  - Валидации / обработка данных

- **Избегай вложенных регионов** — это ухудшает восприятие и поддержку.

---

✅ **Пример корректного использования:**

```python
# region Импорты
from pathlib import Path
from pydantic import BaseModel
# endregion

# region Константы
MAX_RETRIES = 3
TIMEOUT_SECONDS = 10
# endregion

# region Модели данных
class User(BaseModel):
    id: int
    name: str
    email: str
# endregion

# region FUNCTION fetch_data_from_api
def fetch_data_from_api(url: str) -> dict:
    """Загружает данные по API."""
    ...
# endregion FUNCTION fetch_data_from_api

# region FUNCTION process_user_data
def process_user_data(data: dict) -> User:
    """Преобразует JSON-данные в объект User."""
    ...
# endregion FUNCTION process_user_data

# region Точка входа
if __name__ == "__main__":
    ...
# endregion
```

---

❌ **Плохие примеры:**

```python
# region FUNCTION
def some_func():
    ...
# endregion FUNCTION
```

❌ Нет имени функции — усложняет поиск и навигацию.

```python
# region func1
def func1():
    ...
def func2():
    ...
# endregion func1
```

❌ Нарушена структура: несколько функций внутри одного региона.

---

📌 **Итог:**

- Используй регионы всегда, даже для одной функции в модуле.
- Каждая функция должна быть выделена как `# region FUNCTION <имя>` и завершаться `# endregion FUNCTION <имя>`.
- Это обязательное требование в проекте — как типизация или логирование.

> AI должен генерировать код только в таком формате.

Если нужно — могу создать **pre-commit хук**, который будет проверять наличие регионов вокруг функций.

---
## 7. Контракты функций: правило размещения и структура

Чтобы ускорить понимание и использование функций, каждый метод должен сопровождаться **явным описанием его контракта**. Это позволяет понять, как использовать функцию, без необходимости читать её тело или полную документацию (`docstring`).

---

### 🔹 Правило для Python: Блок `# CONTRACT`
В Python-коде контракт пишется в виде **специального блока комментариев** сразу после директивы `# region FUNCTION <имя_функции>`, но до строки определения `def`.

#### ✅ Структура блока `# CONTRACT`
```python
# CONTRACT
# Args:
#   - <имя_параметра>: <краткое описание смысла параметра>
# Returns:
#   - <описание возвращаемого значения или 'None'>
# Side Effects:
#   - <описание побочных эффектов, например, 'Запись в файл', 'Вызов API'>
# Raises:
#   - <тип ошибки>: <условие её возникновения>
# Tests:
#   - <описание теста>: <ожидаемый результат>
```

- **Args**: Входные параметры (предусловия)
- **Returns**: Возвращаемое значение (часть постусловий)
- **Side Effects**: Побочные действия, влияющие на состояние вне функции
- **Raises**: Явные исключения, которые может выбросить функция
- **Tests**: Примеры вызовов функции с ожидаемыми результатами

#### ✅ Пример реализации на Python
```python
# region FUNCTION save_user_data
# CONTRACT
# Args:
#   - user: Объект пользователя для сохранения.
#   - output_path: Путь к файлу для записи данных.
# Returns:
#   - None
# Side Effects:
#   - Создает или дописывает данные в файл по пути 'output_path'.
# Raises:
#   - FileNotFoundError: если директория для 'output_path' не существует.
# Tests:
#   - user=User("Alice"), output_path=Path("/tmp/u.txt"): файл должен содержать строку 'Alice'.
#   - output_path=Path("/nonexistent/dir/u.txt"): выбрасывается FileNotFoundError

def save_user_data(user: User, output_path: Path) -> None:
    """
    Сохраняет данные одного пользователя в указанный файл.

    Полная документация находится здесь, внутри docstring,
    и описывает детали реализации, контекст и примеры использования.
    """
    try:
        with output_path.open("a", encoding="utf-8") as f:
            f.write(str(user) + "\n")
    except FileNotFoundError as e:
        logger.error(f"Директория для файла {output_path} не найдена.")
        raise e
# endregion FUNCTION save_user_data
```

---

### 🔹 Правило для JavaScript/TypeScript: Комментарий JSDoc
В JavaScript и TypeScript контракт функции оформляется с помощью **блока комментариев JSDoc** (`/** ... */`) непосредственно перед её объявлением.

#### ✅ Структура блока JSDoc
```js
/**
 * @param {<тип>} <имя_параметра> - <Описание параметра и его предусловия>
 * @returns {<тип>} <Описание возвращаемого значения и его постусловия>
 * @throws {<тип ошибки>} <Условие возникновения ошибки>
 * @pre <Текстовое описание предусловия>
 * @post <Текстовое описание постусловия>
 * @test <Описание теста>: <Ожидаемый результат>
 */
```

#### ✅ Пример реализации на JavaScript
```js
/**
 * Вычисляет скидку на товар.
 *
 * @param {number} price - Цена товара. Должна быть положительным числом.
 * @param {number} discount - Процент скидки. Должен быть в диапазоне [0, 100].
 * @returns {number} Цена со скидкой.
 * @throws {Error} Если цена или скидка не соответствуют предусловиям.
 * @pre price > 0
 * @pre discount >= 0 && discount <= 100
 * @post Возвращаемое значение >= 0
 * @test price=100, discount=20: результат=80
 * @test price=100, discount=0: результат=100
 */
function calculateDiscountedPrice(price, discount) {
  if (typeof price !== 'number' || price <= 0) {
    throw new Error('Цена должна быть положительным числом.');
  }
  if (typeof discount !== 'number' || discount < 0 || discount > 100) {
    throw new Error('Скидка должна быть в диапазоне от 0 до 100.');
  }

  const result = price - (price * discount / 100);

  if (result < 0) {
      throw new Error('Результат не может быть отрицательным.');
  }

  return result;
}
```

---

### ⚠️ Общие принципы и ошибки

#### ❌ Пример: нарушение правила
```python
# region FUNCTION save_user_data
def save_user_data(user: User, output_path: Path) -> None:
    ...
# endregion FUNCTION save_user_data
```
**Ошибка**: отсутствует блок контракта (`# CONTRACT` или `JSDoc`). Чтобы понять, как вызвать функцию, нужно заглядывать внутрь или читать полную документацию, что замедляет разработку.

---

### 📌 Связь краткого контракта и полной документации
- **Краткий контракт** (`# CONTRACT` / `JSDoc`) — это **резюме для быстрого использования**: "Что мне нужно знать, чтобы вызвать эту функцию?"
- **Полная документация** (`docstring`) — это **детальное описание**: "Как и почему эта функция работает именно так?"

---

### ✅ Резюме
- Контракт должен **идти перед определением функции**.
- Это **обязательная часть стиля кода** для улучшения его читаемости.
- Он делает код **самодокументируемым** и ускоряет его понимание.
- Использование контракта **обязательно для каждой функции**, независимо от её размера.
- В контракт необходимо включать **примеры тестов с ожидаемыми результатами**, что облегчает верификацию корректности реализации.



---

## 8. Добавляй логгер
Пиши логи с семантическими якорями со значениями [START_FUNCTION][Название функции], [END_FUNCTION][Название функции].
Обозначай в логах для каждого лога название функции и логический блок внутри функции. 

---

## 9. При изменении кода ВСЕГДА перепроверяй и при необходимости изменяй:
- **Комментарии**
- **module docstring**
- **Docs к функциям**
- **Контракты**
- **Модели данных**
- **Техническое задание** (если есть)

